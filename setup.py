from setuptools import setup

setup(
   name='HousePricePackage',
   version='0.1.0',
   author='Refuna',
   author_email='example@example.com',
   packages=['HousePricePackage'],
   scripts=[],
   url='http://pypi.python.org/pypi/PackageName/',
   license='LICENSE',
   description='An awesome package that predict house price',
   long_description=open('README.md').read(),
   install_requires=[
   ],
)

